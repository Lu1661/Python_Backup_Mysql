A_BACKUP_BD

Spanish:
En la linea 8, buscar la ruta de mysqldump, si es otra ruta reemplazarlo Aqui
Desde la linea 11 a la 14 reemplazar con tus credenciales del acceso al mysql
En la linea 14, la password va con una f por delante de las comillas simples si es que tu password tiene una @,
sino lo puedes omitir
En la linea 18 debes reemplazar por la ruta donde se guardara el archivo.sql

English:
On line 8, look for the path of mysqldump, if it's another path replace it here
From lines 11 to 14 replace with your mysql login credentials
On line 14, the password goes with an f in front of the single quotation marks if your password has an @,
if not, you can skip it
On line 18 you must replace with the path where the .sql file will be saved
--------------------------------------------------------


A_BACKUP_TABLAS.txt

Spanish:
En la linea 2 reemplazar la ruta donde se guardo el backup.sql
copiar el codigo en la terminal, ahi se podra revisar en la terminal las tablas que han sido backupeadas

En la linea 5 reemplazar la ruta donde se guardo el backup.sql
copiar el codigo en la terminal, ahi se podra guardar un archivo txt en el cual estan las tablas backupeadas, pero ya no se veran en la terminal

English:
On line 2 replace the path where the backup.sql was saved
Copy the code in the terminal, there you will be able to review the tables that have been backed up in the terminal

On line 5 replace the path where the backup.sql was saved
Copy the code in the terminal, there you can save a txt file in which the tables are backed up, but they will no longer be seen in the terminal